const UseRef = () => {
    return (
        <div className="demo-container">
            <div className="demo-box" style={{display:'flex',justifyContent:'center',alignItems:'center'}}>Has already been used in Forward Refs. Check evernote for further details on the hook.</div>
        </div>
    )
}

export default UseRef